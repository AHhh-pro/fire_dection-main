# 工厂安全监控系统 - 使用与开发文档

## 第一部分：如何运行代码

### 一、环境准备

#### 1.1 安装MySQL数据库
1. 下载MySQL Installer：https://dev.mysql.com/downloads/installer/
2. 安装时选择"Server only"
3. 设置root密码为：`123456`（与代码配置一致）
4. 确保MySQL服务已启动

#### 1.2 安装Python依赖
在项目根目录打开命令行，执行：
```bash
pip install -r requirements.txt
```

主要依赖包括：
- PyQt5（GUI界面）
- ultralytics（YOLO检测）
- PyMySQL（数据库连接）
- opencv-python（图像处理）

#### 1.3 初始化数据库
运行数据库初始化脚本：
```bash
python init_database.py
```

此脚本会：
- 创建数据库 `factory_safety`
- 创建用户表 `users`
- 创建报警表 `alerts`
- 创建登录日志表 `login_logs`
- 创建默认管理员账户（用户名：admin，密码：admin123）

### 二、启动程序

#### 2.1 运行主程序
```bash
python main.py
```

#### 2.2 登录系统
- 使用默认管理员账户登录：
  - 用户名：`admin`
  - 密码：`admin123`
- 或点击"注册账户"创建新用户

#### 2.3 开始检测
1. 选择输入源（摄像头/视频文件/图片文件）
2. 选择检测模型
3. 调整置信度（默认0.25）
4. 点击"开始检测"

---

## 第二部分：项目详细介绍

### 一、项目结构

```
fire_dection-main/
├── main.py                      # 程序入口，管理登录和主窗口
├── yolo_detector_gui.py         # 主检测界面（核心文件）
├── login_window.py              # 登录注册界面
├── auth_manager.py              # 用户认证管理
├── database.py                  # 数据库操作模块
├── init_database.py             # 数据库初始化脚本
├── alert_view_window.py         # 报警查看窗口
├── utils.py                     # 工具函数
├── train.py                     # YOLO模型训练脚本
├── requirements.txt             # Python依赖列表
├── weights/                     # 模型权重文件目录
│   ├── yolov8n.pt              # YOLOv8 Nano模型
│   ├── yolov8s.pt              # YOLOv8 Small模型
│   ├── best.pt                 # 烟雾火灾检测模型
│   └── helmet_vest_model.pt    # 人员安全检测模型
└── runs/                        # 训练和检测结果目录
```

### 二、核心文件功能说明

#### 2.1 main.py - 程序入口
- 检查数据库连接
- 启动登录窗口
- 登录成功后跳转到主界面
- 管理窗口生命周期

#### 2.2 yolo_detector_gui.py - 主检测界面（核心）
包含以下主要功能模块：
- 视频/摄像头/图片检测
- 安全警报显示
- 温度数据接收
- 报警数据库存储

#### 2.3 login_window.py - 登录注册界面
- 用户登录表单
- 用户注册表单
- 表单验证

#### 2.4 database.py - 数据库模块
- 数据库连接管理
- 用户数据操作
- 报警数据操作

#### 2.5 alert_view_window.py - 报警查看窗口
- 显示历史报警记录
- 筛选和查询报警
- 处理报警状态

### 三、安全警报系统详解

系统包含**两大类报警**：生产环境安全报警 和 生产人员安全报警

#### 3.1 生产环境安全报警

##### 3.1.1 火警报警

**检测目标：**
- `fire`（明火/火焰）
- `smoke`（烟雾）

**报警级别规则：**

| 检测情况 | 报警级别 | 报警消息 |
|---------|---------|---------|
| 同时检测到明火和烟雾 | 3级（最高） | "火警三级：检测到明火和烟雾" |
| 仅检测到明火 | 2级 | "火警二级：检测到明火" |
| 仅检测到烟雾 | 1级 | "火警一级：检测到烟雾" |
| 均未检测到 | 0级（安全） | "生产环境安全" |

**判断逻辑代码：**
```python
if fire > 0 and smoke > 0:
    level = 3  # 最高级
elif fire > 0:
    level = 2  # 二级
elif smoke > 0:
    level = 1  # 一级
else:
    level = 0  # 安全
```

##### 3.1.2 高温报警

**数据来源：**
- HTTP服务器接收温度数据（端口8090）
- C#客户端或其他设备发送温度数据

**温度分级规则：**

| 温度范围 | 报警级别 | 状态显示 |
|---------|---------|---------|
| ≤ 30°C | 0级 | 温度正常（绿色） |
| 30°C ~ 50°C | 2级 | 温度偏高（橙色） |
| > 50°C | 3级 | 温度过高（红色） |

**数据格式：**
```json
{
    "sensorId": "temp_001",
    "value": 45.5
}
```

#### 3.2 生产人员安全报警

##### 3.2.1 检测目标

模型输出以下类别：
- `person`（人员）
- `helmet`（安全帽）
- `vest`（反光背心）
- `glove`（手套）

##### 3.2.2 复选框设置（重要）

在左侧控制面板的"模型设置"区域，有"生产人员安全检测"分组框，包含三个复选框：

- ☑️ **安全帽**（默认勾选）
- ☑️ **反光背心**（默认勾选）
- ☐ **手套**（默认不勾选）

**只有勾选了的项目才会进行检测和报警！**

##### 3.2.3 报警产生规则

**核心逻辑：**
比较 `person` 数量和各装备数量

**报警条件：**
- 如果 `helmet` 数量 < `person` 数量 → 产生安全帽报警
- 如果 `vest` 数量 < `person` 数量 → 产生反光背心报警
- 如果 `glove` 数量 < `person` 数量 → 产生手套报警

**报警消息格式：**
- "X人未佩戴安全帽（共Y人）"
- "X人未穿戴反光背心（共Y人）"
- "X人未佩戴手套（共Y人）"

**示例场景：**

| 检测到 | 勾选项目 | 报警结果 |
|-------|---------|---------|
| 2人，1个安全帽，2件背心 | 安全帽+背心 | "1人未佩戴安全帽" |
| 3人，3个安全帽，2件背心 | 安全帽+背心 | "1人未穿戴反光背心" |
| 2人，2个安全帽 | 仅安全帽 | 无报警（全部合规） |
| 2人，0个安全帽 | 安全帽+手套 | "2人未佩戴安全帽"（手套不报警，因为数量为0但勾选） |

**注意：** 如果某装备数量为0且未勾选，则不报警；如果勾选但数量为0，则全部人员都算未佩戴。

### 四、报警防重复机制（重要）

#### 4.1 为什么需要防重复？

在视频或摄像头实时检测时，每秒钟会处理多帧图像。如果不加控制，同一报警会每秒都被保存到数据库，产生大量重复记录。

#### 4.2 防重复规则

**区分检测类型：**

| 检测类型 | 防重复机制 |
|---------|-----------|
| 图片检测 | 不启用，每次检测都保存报警 |
| 视频检测 | 启用，防止重复保存 |
| 摄像头检测 | 启用，防止重复保存 |

**防重复逻辑：**

对于实时检测（视频/摄像头）：

| 报警变化情况 | 是否保存 | 说明 |
|-------------|---------|------|
| 首次出现该类型报警 | ✅ 保存 | 记录初始状态 |
| 报警级别升高 | ✅ 保存 | 如1级→2级、2级→3级 |
| 报警级别不变 | ❌ 不保存 | 60秒内只保存一次 |
| 报警级别降低 | ❌ 不保存 | 如3级→2级，需等60秒 |

**冷却时间：** 60秒

**示例流程：**
```
时间轴：
0秒   - 检测到烟雾（1级）→ 保存到数据库
5秒   - 仍检测到烟雾（1级）→ 不保存（冷却期内）
10秒  - 检测到明火+烟雾（3级）→ 保存（级别升高）
20秒  - 仍检测到明火+烟雾（3级）→ 不保存（冷却期内）
70秒  - 仍检测到明火+烟雾（3级）→ 保存（冷却期过）
80秒  - 仅检测到烟雾（1级）→ 不保存（级别降低，等冷却）
130秒 - 仅检测到烟雾（1级）→ 保存（冷却期过）
```

#### 4.3 查看防重复日志

在信息面板中可以看到以下日志：
- `"实时检测模式：报警防重复机制已启用（冷却期60秒）"`
- `"[fire] 报警级别升高 (1级→2级)，立即保存"`
- `"[fire] 报警级别保持不变 (2级→2级)，冷却中还需 45 秒"`
- `"火警报警处于冷却期，跳过保存"`

### 五、界面布局说明

#### 5.1 主界面布局

```
┌─────────────────────────────────────────────────────────┐
│  菜单栏（文件、视图、帮助）                               │
├──────────────────┬──────────────────────────────────────┤
│                  │                                      │
│   左侧面板        │           中央显示区                  │
│   （控制区）      │           （图像显示）                │
│                  │                                      │
│  ┌────────────┐  │  ┌────────────────────────────────┐  │
│  │ 输入源选择  │  │  │                                │  │
│  │ - 摄像头   │  │  │      图像/视频显示区域           │  │
│  │ - 视频文件 │  │  │                                │  │
│  │ - 图片文件 │  │  │                                │  │
│  └────────────┘  │  └────────────────────────────────┘  │
│  ┌────────────┐  │  ┌────────────────────────────────┐  │
│  │ 模型设置    │  │  │      信息面板（日志输出）        │  │
│  │ - 模型选择 │  │  │                                │  │
│  │ - 置信度   │  │  └────────────────────────────────┘  │
│  │ - 人员安全 │  │                                      │
│  │   复选框   │  │                                      │
│  └────────────┘  │                                      │
│  ┌────────────┐  │                                      │
│  │ 检测控制    │  │                                      │
│  │ - 开始检测 │  │                                      │
│  │ - 停止检测 │  │                                      │
│  └────────────┘  │                                      │
│  ┌────────────┐  │                                      │
│  │ 检测统计    │  │                                      │
│  │ （类别数量）│  │                                      │
│  └────────────┘  │                                      │
│  ┌────────────┐  │                                      │
│  │ 安全警报    │  │                                      │
│  │ - 生产环境 │  │                                      │
│  │ - 生产人员 │  │                                      │
│  └────────────┘  │                                      │
│  ┌────────────┐  │                                      │
│  │ 区域温度    │  │                                      │
│  └────────────┘  │                                      │
│                  │                                      │
└──────────────────┴──────────────────────────────────────┘
```

#### 5.2 菜单功能

**文件菜单：**
- 打开图片（Ctrl+O）
- 打开视频（Ctrl+V）
- 摄像头开关（Ctrl+C）
- 退出（Esc）

**视图菜单：**
- 显示/隐藏统计信息
- 查看报警记录（打开报警查看窗口）

**检测菜单：**
- 开始检测
- 停止检测

#### 5.3 报警查看窗口

通过菜单"视图"→"查看报警记录"打开，功能包括：
- 显示所有历史报警
- 按类别/级别/状态筛选
- 按日期范围查询
- 处理报警（标记为已处理）
- 分页显示
- 自动刷新（30秒）

### 六、数据库说明

#### 6.1 数据库配置

配置文件：`database.py` 中的 `DatabaseConfig` 类

```python
HOST = 'localhost'      # 数据库地址
PORT = 3306            # 端口
USER = 'root'          # 用户名
PASSWORD = '123456'    # 密码
DATABASE = 'factory_safety'  # 数据库名
```

#### 6.2 主要数据表

**users表** - 用户信息
- id: 用户ID
- username: 用户名
- password_hash: 密码哈希
- salt: 盐值
- role: 角色（admin/user/operator）
- status: 状态（active/inactive/locked）

**alerts表** - 报警记录
- id: 报警ID
- alert_type: 报警类型（fire/smoke/temperature/helmet/vest/glove）
- alert_category: 类别（environment/personnel）
- alert_level: 级别（0-3）
- alert_message: 报警消息
- detected_objects: 检测到的对象（JSON格式）
- status: 状态（active/resolved）
- created_at: 创建时间
- resolved_by: 处理人

### 七、温度数据接收

#### 7.1 HTTP服务器

- 端口：8090
- 地址：http://localhost:8090

#### 7.2 数据格式

发送POST请求，Content-Type: application/json

请求体：
```json
{
    "sensorId": "temp_001",
    "value": 45.5
}
```

#### 7.3 C#发送示例

```csharp
using (var client = new HttpClient())
{
    var data = new { sensorId = "temp_001", value = 45.5 };
    var json = JsonConvert.SerializeObject(data);
    var content = new StringContent(json, Encoding.UTF8, "application/json");
    var response = await client.PostAsync("http://localhost:8090", content);
}
```

### 八、常见问题

#### 8.1 数据库连接失败
- 检查MySQL服务是否启动
- 检查密码是否为123456
- 运行 `python init_database.py`

#### 8.2 模型加载失败
- 检查weights/目录是否有.pt文件
- 检查模型文件是否完整

#### 8.3 温度数据不显示
- 检查8090端口是否被占用
- 检查防火墙设置

#### 8.4 报警不保存
- 检查是否为实时检测模式
- 查看信息面板的防重复日志
- 检查数据库连接

---

**文档版本：** v2.0  
**更新日期：** 2024年
